using UnityEngine;
using System.Collections;

public class Variables : MonoBehaviour {

    // Class in progress.
    // Intent being a single place to easily modify all variables straight from Unity.
    // This should streamline testing & developement.

    public enum lane
    {
        left,
        middle,
        right
    }

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
